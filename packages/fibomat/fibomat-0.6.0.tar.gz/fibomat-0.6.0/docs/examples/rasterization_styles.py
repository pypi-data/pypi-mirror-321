# Ignore the following lines. These are used to adjust the plot for the documentation.
import sys
if 'sphinx-build' in sys.argv:
    _fullscreen = False
else:
    _fullscreen = True

from fibomat import Sample, Vector, U_
from fibomat import default_backends, shapes, curve_tools

sample = Sample()

# two_dim,LineByLine, conseq.

# two_dim,LineByLine, cross

# two_dim,LineByLine, serpentine

# two_dim,LineByLine, double serpentine

# two_dim,LineByLine, back-stitch

# two_dim, contour, conseq.

# two_dim, contour, cross

# two_dim, contour, serpentine

# two_dim, contour, double serpentine

# two_dim, contour, back-stitch



