[project]
name = "troop"
version = "0.0.1"
description = "A Python package"
authors = []
dependencies = [
    "hatchling>=1.27.0",
    "twine>=6.0.1",
]
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
