[tool.poetry]
name = "package_203_project"
version = "v0.3.0"
description = "Package created for the Python course project"
authors = ["Maelys Malichecq"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
