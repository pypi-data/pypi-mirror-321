{% set character_greeting = get_character_greeting(character) %}
- name: "character original greeting"
  content: |
    {{ settings.start_token }}{{ escape_special_characters(character_greeting) }}

- name: "rephrase greeting prompt"
  content: |
    {{ settings.start_token }}{{ escape_special_characters(dynamic_greeting_prompt) }}
