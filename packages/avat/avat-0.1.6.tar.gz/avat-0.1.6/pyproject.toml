[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "avat"
version = "0.1.6"
authors = [
  { name="lorepieri", email="lorepieri@hotmail.it" },
]
description = "AI avatars"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
