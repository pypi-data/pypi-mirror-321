[project]
name = "remembercli"
version = "0.1.0"
description = "A CLI-based task management tool for the distracted!"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["dateparser>=1.2.0", "typer>=0.15.1"]

[project.scripts]
rem = "rememberCLI.main:app"


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
