[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "csmp"
version = "0.1.0"
authors = [{ name = "xephosbot", email = "xephosbot@gmail.com" }]
description = "Library for compressive sensing matching pursuit"
dependencies = ["numpy", "scipy"]