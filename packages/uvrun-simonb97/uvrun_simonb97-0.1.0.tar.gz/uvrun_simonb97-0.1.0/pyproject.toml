[project]
name = "uvrun_simonb97"
version = "0.1.0"
authors = [{name = "Simon Benedict", email = "hi@sbenedict.dev"}]
description = "Run Python scripts with inline metadata directly from URLs"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "click>=8.1.7",
    "tomli-w>=1.0.0",
    "rich>=13.7.0",
    "requests>=2.31.0",
]

[project.optional-dependencies]
dev = [
    "pre-commit>=3.6.0",
    "ruff>=0.1.14",
    "build>=1.0.3",
    "twine>=4.0.2",
]

[project.scripts]
uvrun = "uvrun_simonb97.cli:main"

[build-system]
requires = ["hatchling", "build"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/uvrun_simonb97"]

[tool.semantic_release]
version_variables = ["src/uvrun_simonb97/__init__.py:__version__"]
version_toml = ["pyproject.toml:project.version"]
commit_message = "chore: release {version}"
branch = "main"
build_command = "pip install build && python -m build"

[tool.ruff]
target-version = "py311"
line-length = 100
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "UP", # pyupgrade
]
ignore = []

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"