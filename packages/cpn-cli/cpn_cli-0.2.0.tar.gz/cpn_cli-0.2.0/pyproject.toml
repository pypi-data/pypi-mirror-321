[project]
authors = [
  { name = "KevinNitroG", email = "kevinnitro@duck.com" },
  { name = "NTGNguyen", email = 'ntgnguyen@duck.com' },
]
description = "cpn cli"
name = "cpn-cli"
version = "0.2.0"
requires-python = ">=3.13"
dependencies = ["cpn-core[discord,ocr]==0.3.0"]

[project.scripts]
cpn-cli = "cpn_cli:__main__.main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cpn_cli"]

[dependency-groups]
dev = ["commitizen>=4.1.0", "git-cliff>=2.7.0", "pre-commit>=4.0.1"]
