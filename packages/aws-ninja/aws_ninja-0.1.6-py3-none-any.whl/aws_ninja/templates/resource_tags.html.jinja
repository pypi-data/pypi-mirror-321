{% if tags.values() %}
    <strong>Resource Tags</strong>

    <dl>
        {% for key, value in tags.items() %}
            <dt>{{ key }}</dt>
            <dd>{{ value }}</dd>
        {% endfor %}
    </dl>
{% endif %}
