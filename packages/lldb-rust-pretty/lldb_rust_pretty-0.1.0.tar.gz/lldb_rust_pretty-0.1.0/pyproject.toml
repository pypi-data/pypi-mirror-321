[project]
name = "lldb-rust-pretty"
version = "0.1.0"
description = "A pretty printer for Rust types in LLDB"
readme = "README.md"
authors = [{ name = "oneofthezombies", email = "hunhoekim@gmail.com" }]
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
