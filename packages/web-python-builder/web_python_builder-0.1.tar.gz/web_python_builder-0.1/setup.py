from setuptools import setup, find_packages

setup(
    name="web_python_builder",
    version="0.1",
    packages=find_packages(),
    description="A simple library for generating HTML files with Python",
    author="Basel",
)
