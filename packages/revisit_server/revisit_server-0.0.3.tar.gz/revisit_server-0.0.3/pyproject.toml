[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "revisit_server"
version = "0.0.3"
dependencies = []
readme = "README.md"

[tool.hatch.build]
only-packages = true
exclude = ["tests"]

[tool.hatch.metadata]
package-data = { "revisit_server" = ["static/*"] }