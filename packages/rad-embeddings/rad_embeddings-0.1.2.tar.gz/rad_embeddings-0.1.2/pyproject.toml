[project]
name = "rad-embeddings"
version = "0.1.2"
description = "RAD Embeddings"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dfa-gym>=0.1.0",
    "stable-baselines3>=2.4.1",
    "tensorboard>=2.18.0",
    "token-env>=0.2.1",
    "torch<2.3.0",
    "torch-geometric>=2.6.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
