[project]
name = "dfa-samplers"
version = "0.1.0"
description = "Deterministic Finite Automata (DFA) samplers"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dfa>=4.7.1",
    "dfa-mutate>=0.1.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
