[tool.poetry]
name = "dtx-apis-prompts-utils"
version = "0.2.1"
description = "Detoxio APIs Prompts parsing and processing utils"
authors = ["jchauhan <jitendra@detoxio.ai>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.10,<4.0"
pydantic = "^2.10.4"
protobuf = ">=3.20.3"


[tool.poetry.group.dev.dependencies]
pytest = "^8.3.4"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
