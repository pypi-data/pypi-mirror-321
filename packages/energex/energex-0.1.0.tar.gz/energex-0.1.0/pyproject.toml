[project]
name = "energex"
version = "0.1.0"
description = "Energy derivatives data collection and storage system"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "polars",
    "yfinance",
    "duckdb"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"