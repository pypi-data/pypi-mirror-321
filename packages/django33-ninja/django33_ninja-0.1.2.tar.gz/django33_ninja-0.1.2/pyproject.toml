[tool.poetry]
name = "django33-ninja"
version = "0.1.2"
description = "django33-ninja 是一个使用Django和Python 3.6+ 类型提示构建api的web框架。"
authors = ["zhangdapeng <1156956636@qq.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pydantic = "^2.10.5"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
