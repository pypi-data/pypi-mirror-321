[tool.poetry]
name = "functor-ninja"
version = "0.4.0"
description = "Functors and Monads for data processing"
authors = ["Felipe Ortega <felipeortegabustamante@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.3"


[[tool.poetry.source]]
name = "PyPI"
priority = "primary"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
